[project]
name = "torkai"
version = "0.1.0"
description = "From-scratch mini GPT style Bangla-English AI model"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
